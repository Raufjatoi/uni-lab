# 1. Open Python IDLE terminal and create a new file. Name it "lab8_1.py". Write a program to demonstrate
# the use of the len() function. Take a user input string and print its length.
s = input('Enter ur name : ')
def find_length (x):
    return len(x)
print(find_length(s))