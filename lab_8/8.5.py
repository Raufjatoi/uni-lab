# 5. Create "lab8_5.py". Use the abs() function to find the absolute value of a user-inputted number. Print the
# result.
u = int(input('Enter nnum : '))
def absolute(x):
    return abs(x)
print(f'absolute value is {absolute(u)}')
