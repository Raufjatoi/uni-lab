# 2. Create "lab9_2.py" to ask the user for a specific word. Use the replace() function to replace all
# occurrences of that word with "REPLACED". Print the modified sentence.
i = input('Enter word ')
r = i.replace(i,'replaced')
print(r)