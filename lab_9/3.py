# 3. Create "lab9_3.py". Write a program that takes a string containing comma-separated values (CSV)
# as input and uses the split() function to separate the values into a list. Then, print the list.
c = input('Enter csv : ')
s = c.split(',')
print(s)